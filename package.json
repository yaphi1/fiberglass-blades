{
  "name": "fiberglass-blades",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "tsc --outDir output && cpx \"src/**/*.{html,css,svg,png,json,md}\" output",
    "clean": "rm -rf output",
    "dev": "tsc --outDir output & cpx \"src/**/*.{html,css,svg,png,json,md}\" output --watch"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "cpx": "^1.5.0"
  }
}
